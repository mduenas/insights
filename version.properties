# version.properties — canonical Android version for insights
# Managed for Dueñas Dev Manager / release scripts
# Do not invent values: keep in sync with store releases
versionName=1.0.7
versionCode=8

# iOS reference (not auto-applied to Xcode; preserved for skew tracking)
# marketingVersion=1.0.5
# currentProjectVersion=6
# NOTE: Android versionName (1.0.7) differs from iOS marketingVersion (1.0.5)
